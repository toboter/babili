<% title 'New personal access token' %>

<% content_for :breadcrumbs do %>
  <%= content_tag :li, yield(:title), class: 'active' %>
<% end %>

<p>
  Personal access tokens function like ordinary OAuth access tokens. They can be 
  used instead of a password for accessing data over HTTP(S), or can be used to authenticate to the API.
</p>

<%= render 'form', personal_access_token: @personal_access_token %>
