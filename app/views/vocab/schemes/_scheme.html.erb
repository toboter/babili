<%= link_to vocab_scheme_path(scheme), class: 'list-group-item' do %>
  <%= content_tag :p, scheme.title, class: 'text-strong' %>
  <%= content_tag :span, polymorphic_url([:vocab, scheme]), class: 'text-small no-link' %>
<% end %>