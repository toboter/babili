toastr["success"]("Successfully applied for <%= @applyment.project.name %>.");
$("#new_project_membership_<%= @applyment.project.id %>").replaceWith("<%= escape_javascript(link_to fa_icon('user', text: 'Awaiting confirmation'), settings_projects_path, class: 'btn btn-default btn-sm text-strong') %>");