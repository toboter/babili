<% (current_person.all_repos-entry.repositories).each do |repo| %>
  <%= content_tag :li do %>
    <%= link_to(repo.name, add_repositories_biblio_entry_path(entry, repository_id: repo.id), method: :post, remote: true) %>
  <% end %>
<% end %>