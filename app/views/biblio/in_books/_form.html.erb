<%= simple_form_for @in_book, 
  html: { class: 'form-horizontal' },
  wrapper: :horizontal_form do |f| %>
  <%= f.error_notification %>
  
  <%= f.input :author_ids, 
    collection: @creators, 
    label_method: :name, 
    value_method: :id, 
    label: 'Authors', 
    selected: @in_book.creatorships.map(&:agent_appellation_id), 
    input_html: {
      multiple: true, 
      id: 'creators_names',
      class: 'creators-selectize'
    } %>
  
  <%= f.input :title, as: :text %>
  
  <hr>

  <%= f.input :book_id, 
    collection: Biblio::Book.all, 
    label_method: :citation, 
    value_method: :id, 
    include_blank: false, 
    label: "Book", 
    selected: @in_book.book.try(:id), 
    input_html: {
      id: 'book_name',
      class: 'book-selectize'
    } %>
  
  <%= f.input :pages %>
  <%= f.input :chapter %>

  <hr>

  <%= f.input :note %>
  <%= f.input :key %>

  <%= f.input :url %>
  <%= f.input :doi %>
  <%= f.input :abstract, as: :text %>
  
  <hr>

  <%= f.input :tag_list, 
    as: :string,
    input_html: {
      value: f.object.tag_list.join(','),
      id: 'vocab_tag_list' }, 
    label: "Tags <span class='text-muted' style='font-weight:normal;'>(separate with comma)</span>".html_safe,
    hint: 'Add tags to categorize the article and make it more discoverable.' %>
  
    <div class="row">
      <div class="col-md-6">
        <hr>
        <%= f.submit class: 'btn btn-primary' %>
        <%= link_to 'Cancel', (@in_book.new_record? ? biblio_entries_path : @in_book), class: "btn btn-default" %>
      </div>
    </div>

<% end %>

<% content_for :modals do %>
  <%= render 'zensus/appellations/new_modal' %>
  <%= render 'biblio/journals/new_modal' %>
<% end %>