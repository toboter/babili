<%= simple_form_for @misc, 
  html: { class: 'form-horizontal' },
  wrapper: :horizontal_form do |f| %>
  <%= f.error_notification %>

  <%= f.simple_fields_for :referencations do |r| %>
    <%= r.hidden_field :repository_id %>
    <%= r.hidden_field :creator_id %>
  <% end %>

  <%= f.input :author_ids, 
    collection: @authors, 
    label_method: :name, 
    value_method: :id, 
    label: 'Authors', 
    selected: @misc.creatorships.map(&:agent_appellation_id), 
    input_html: {
      multiple: true, 
      id: 'creators_names',
      class: 'creators-selectize' #appellations-create-selectize
    } %>
  
  <%= f.input :title, as: :text %>

  <%= f.input :howpublished %>

  <%= f.input :year %>
  <%= f.input :month %>

  <hr>
  
  <%= f.input :note %>
  <%= f.input :key %>

  <hr>

  <%= f.input :url %>
  <%= f.input :doi %>
  <%= f.input :abstract, as: :text %>
  
  <hr>

  <%= f.input :tag_list, 
    as: :string,
    input_html: {
      value: f.object.tag_list.join(','),
      id: 'vocab_tag_list' }, 
    label: "Tags <span class='text-muted' style='font-weight:normal;'>(separate with comma)</span>".html_safe,
    hint: 'Add tags to categorize the misc and make it more discoverable.' %>
  
    <div class="row">
      <div class="col-md-6">
        <hr>
        <%= f.submit class: 'btn btn-primary' %>
        <%= link_to 'Cancel', (@misc.new_record? ? namespace_repository_biblio_references_path(@entry.referencations.last.repository.owner, @entry.referencations.last.repository) : @misc), class: "btn btn-default" %>
      </div>
    </div>

<% end %>

<% content_for :modals do %>
  <%= render 'zensus/appellations/new_modal' %>
<% end %>