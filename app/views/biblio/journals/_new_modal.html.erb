  <div class="modal fade journal-modal" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel">
    <div class="modal-dialog modal-md" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
          <h4 class="modal-title" id="myLargeModalLabel">Create a new journal</h4>
        </div>
        <div class="modal-body biblio">
          <%= simple_form_for Biblio::Journal.new do |f| %>
            <div class="row">
              <div class="col-md-12">
                <%= f.error_notification %>
                <%= f.input :name %>
                <%= f.input :abbr %>
                <hr>
                <%= f.input :key %>
                <%= f.input :note, as: :text %>
                <%= f.input :print_issn %>

              </div>
              <div class="col-md-12 actions --spacious">
                <%= f.submit class: 'btn btn-primary' %>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>